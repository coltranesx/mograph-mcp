[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mograph-mcp"
version = "0.1.0"
description = "Puppeteer for After Effects. Use AE with Claude Code to make production-ready videos."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Arman Luthra" }]
keywords = ["after-effects", "vfx", "motion-graphics", "extendscript", "mcp", "claude", "automation"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Multimedia :: Video",
  "Environment :: Console",
]

[project.urls]
Homepage = "https://github.com/coltranesx/mograph-mcp"
Repository = "https://github.com/coltranesx/mograph-mcp"
Issues = "https://github.com/coltranesx/mograph-mcp/issues"

[project.scripts]
mograph-mcp = "mograph_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["mograph_cli"]
